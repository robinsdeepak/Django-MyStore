from django.shortcuts import render, HttpResponse


def SetLocationView(request):
    return HttpResponse('We are working on this function now')


def Tracking(request):
    return HttpResponse('We are working on this function now')
